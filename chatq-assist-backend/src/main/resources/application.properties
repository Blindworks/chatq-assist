# Application
spring.application.name=chatq-assist

# Database
#spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:chatq_assist}
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:chatq_assist}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:taxcRH51#}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# Server
server.port=8080

# OpenAI Configuration
openai.api.key=${OPENAI_API_KEY:your-api-key-here}
openai.model.chat=gpt-4
openai.model.embedding=text-embedding-3-small

# Logging
logging.level.com.chatq.assist.service.ChatServiceLLM=DEBUG
logging.level.dev.langchain4j=DEBUG
